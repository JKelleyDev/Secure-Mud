This is a python based MUD which uses TLS for secure connections, encrypted PBKDF2 password instead of plain text storage, and uses custom story lines passed from story.txt files. 

This project uses Python 3.13+, pip package manager, and cryptography for passwords. The database will be POSTGRESQL coupled with alembic and sqlalchemy. 

Use classes where possible for inheritance. Use text files for story management. 
